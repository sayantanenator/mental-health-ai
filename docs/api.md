# API Documentation

This document describes the Mental Health AI API endpoints.

- Base URL: /api/v1
- Auth: Bearer token (JWT) recommended

## Endpoints

- POST /analysis: Analyze text/audio input
- POST /batch: Process batch jobs
- GET /admin/health: Health check

See inline docstrings in `api/app.py` and route files for details.
